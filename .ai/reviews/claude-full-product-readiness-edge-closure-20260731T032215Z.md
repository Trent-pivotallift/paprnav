Reviewed read-only; no files touched, no secrets/frozen PDFs opened.

**1. `ad_extraction.py:425-452` — rejects empty *and* blank.** `validate_extraction_output` now strips each item and filters falsy values before the emptiness check (`:444-448`), so `[]`, `[""]`, `["   "]`, and `[None]` all raise `ValueError("AD extraction field affectedProducts must contain at least one attributable product")`. The list-type check at `:441-443` still runs first, so non-list payloads keep their own error. Confirmed.

**2. `ads.py:309-322` — 422 before any approval side effect.** `validate_extraction_output(decision_output)` is the *first* statement inside the `approved|edited` branch and its `ValueError` is converted to `HTTP_422_UNPROCESSABLE_CONTENT` with the message as detail (`:312-316`). Every mutation is strictly downstream of it: `extraction.output`/`status = "approved"`, `directive.extraction_status/review_status/approved_at` (`:317-321`), `populate_applicability_from_extraction` + fan-out (`:323-345`), and the shared tail (`review.status`, `decision`, `reviewer_user_id`, `reviewed_at`, the per-aircraft `resolve_aircraft_ad_coverage`/`invalidate_aircraft_match_results` loop, and the product event) at `:352-375`. The raise exits the handler before the session commits, so the review stays undecided and no directive is marked approved. The pre-computed `affected_aircraft_ids` query at `:301-308` is a read only. Confirmed.

**3. `test_ad_ingestion.py:139-183` — direct proof, in the right order.** In `test_ad_extraction_routes_low_confidence_output_to_review`, the empty-applicability decision is posted *first* (`:140-148`), asserting `422` and the `"at least one attributable product"` detail (`:149-150`). Only then is the same review re-posted with `affectedProducts = ["Cessna 172R"]` (`:152-156`), and the fan-out is proven on the succeeding call: non-empty `ADTargetApplicability.target_id` set (`:161-167`), an `ADCoverageSubscription` joined through `ADCoverageSet.target_id` for that aircraft (`:168-176`), an `ad_matching_invalidated` product event (`:177-182`), and the owner-visible worklist returning `pending_recomputation` / `reprocessingRequired: true` (`:184-192`). Because the second post reuses the *same* review id and succeeds, the test also implicitly proves the 422 left the review undecided — otherwise `:297` would have returned 400 "Review has already been decided". Confirmed.

**Blockers: 0.** The residual empty-`affectedProducts` path from the prior review is closed — the matcher's "empty products applies to all" behavior (`ad_matching.py:342-345`) is now unreachable for approved extractions, since no extraction can reach `approved` without at least one attributable product.

Two non-blocking observations, offered only as notes: the test exercises `[]` but not the whitespace-only variant (`[" "]`), which is handled in source but uncovered; and the subscription assertion at `:168-176` does not constrain `status == "active"`, while the route's fan-out does (`ads.py:344`). Neither weakens the closure.

Ready on this edge.
